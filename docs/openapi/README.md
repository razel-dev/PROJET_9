# OpenAPI

- Placer ici un fichier par service (ex: patient-service.yaml, history-service.yaml, assessment-service.yaml).
- La CI valide la compatibilité (diff sémantique) avant merge.
- Les clients générés sont produits dans libs/clients.